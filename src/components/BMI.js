import { Box, Heading } from "@chakra-ui/react";
import React from "react";

function BMI({ user }) {
  console.log(user);

  return (
    <Box>
      <Heading>BMI Stuff</Heading>
    </Box>
  );
}

export default BMI;
